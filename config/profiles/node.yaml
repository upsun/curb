# Node.js / npm / yarn / pnpm
# HOME is passed through so ~ paths below resolve to the real home directory.
commands: [node, npm, npx, pnpm, yarn, bun]
domains:
  - registry.npmjs.org
  - "*.npmjs.org"
  - "*.yarnpkg.com"
read:
  - ~/.npm
  - ~/.cache/yarn
  - ~/.local/share/pnpm
write:
  - node_modules
exec:
  - sh
  - node
  - npm
  - npx
env:
  - HOME
  - NODE_ENV
  - NPM_CONFIG_REGISTRY
