# Git / GPG (includes SSH).
# HOME is passed through so ~ paths below resolve to the real home directory.
profiles:
  - ssh
read:
  - ~/.gitconfig
  - ~/.config/git
exec:
  - git
  - gpg
env:
  - HOME
  - GIT_AUTHOR_NAME
  - GIT_AUTHOR_EMAIL
  - GIT_COMMITTER_NAME
  - GIT_COMMITTER_EMAIL
