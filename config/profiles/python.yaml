# Python / pip
# HOME is passed through so ~ paths below resolve to the real home directory.
domains:
  - pypi.org
  - "*.pythonhosted.org"
read:
  - ~/.cache/pip
  - ~/.local/lib
exec:
  - python3
  - pip
  - pip3
env:
  - HOME
  - VIRTUAL_ENV
  - PIP_INDEX_URL
