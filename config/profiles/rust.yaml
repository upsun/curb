# Rust / Cargo
# HOME is passed through so ~ paths below resolve to the real home directory.
domains:
  - crates.io
  - "*.crates.io"
  - static.rust-lang.org
read:
  - ~/.cargo
  - ~/.rustup
exec:
  - cargo
  - rustc
  - rustup
env:
  - HOME
  - CARGO_HOME
  - RUSTUP_HOME
