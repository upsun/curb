# GitHub (includes Git).
# HOME is passed through so ~ paths below resolve to the real home directory.
commands: [gh]
profiles:
  - git
domains:
  - github.com
  - api.github.com
  - raw.githubusercontent.com
exec:
  - gh
env:
  - HOME
  - GH_TOKEN
  - GITHUB_TOKEN
